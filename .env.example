# Локальная разработка / сборка релиза (скопируйте в .env — .env в git не попадает)
SERVER_URL=http://localhost:3000

# Опционально: только при приватной сборке с телеметрией (не коммитить реальные URL)
# TELEMETRY_ENDPOINT_URL=
# RESTORE_ENDPOINT_URL=

# Секреты сервера и бота — только на VPS / в server/.env и telegram-bot/.env, не в клиенте